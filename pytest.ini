[pytest]
# The 29 test_*.py under tests/ are standalone scripts with module-level
# side effects; importing them as pytest modules would explode. Only collect
# the wrapper, which runs each script in its own subprocess.
testpaths = tests
python_files = test_scripts.py
